#!/usr/bin/env sh

youtube-comment-scraper "$1" | highlight --out-format=ansi -S json | less

# youtube-comment-scraper "$1" | highlight -S json --out-format=ansi | grep -e "\"text\"\|\"author\"\|\"likes\"" | less

